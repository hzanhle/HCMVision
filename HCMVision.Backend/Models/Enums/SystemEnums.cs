namespace HcmcRainVision.Backend.Models.Enums
{
    public enum JobStatus { Running, Completed, Failed }
    public enum AttemptStatus { Success, Failed, Error }
    public enum CameraStatus { Active, Maintenance, Offline }
    public enum UserRole { Admin, User }
}
